const config = {
    content: ['./src/**/*.{html,js,svelte,ts}'],

    theme: {
        extend: {
            boxShadow: {
                "header": "0 -3px 10px 0 #32293d"
            }
        },
    },

    plugins: [],
};

module.exports = config;
